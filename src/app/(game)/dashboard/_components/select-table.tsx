import { CardItem } from '@/components/card-item'

interface SelectTableProps {}

export const SelectTable = ({}: SelectTableProps) => {
  return <CardItem title="Select Table" redirectUrl="/dashboard/table" />
}
